. Run the Server
In your first terminal:

Bash

npm run dev
(Wait for it to log 🚀 [SERVER] Успешно запущен...)

2. Run the Test
In a second, separate terminal:

Bash

npx ts-node test-api.ts